<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>
            {% block tab_title %}
                {{ title }}
            {% endblock %}- Livror - Darkirby
        </title>
        <link rel="icon" href=" {{ asset('build/favicon.png') }}" />
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header>
            {# <nav class="bg-light">
                <div class="container">
                    <div class="bg-secondary py-4"></div>
                </div>
            </nav> #}

            <div class="container text-center mb-4 bg-light">
                <h1>
                    {{ title }}
                </h1>
            </div>
        </header>

        <main>
            {% block main %}

            {% endblock %}
        </main>

        <footer>
            <div class="container-fluid">
                <div class="row gw-md-4 fw-light bg-light py-2 mt-4 small">
                    <div class="col-12 col-md-6 text-center text-md-end">
                        Crée par darkirby
                    </div>
                    <div class="col-12 col-md-6 text-center text-md-start">
                        Version {{ app.version }}
                    </div>
                </div>
            </div>
        </footer>
    </body>
</html>
