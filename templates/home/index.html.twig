{% extends 'base.html.twig' %}
{% import 'theme/macro.html.twig' as m %}

{% set title = 'home.index.title'|trans %}

{% block main %}
  <section>
    <div class="container">
      {{ m.attachment_modal() }}

      {{ block('flash', 'theme/flash.html.twig') }}

      {# Search part #}
      <div class="card-max-large card-max-md-extra mx-auto bg-primary">
        <div class="mt-2 border border-primary bg-body rounded" {{ stimulus_controller('search-select') }}>
          <h4 class="text-center bg-primary p-2 mb-0">{{ 'home.index.search.title'|trans }}</h4>
          <div class="input-group p-3" role="group" aria-label="{{ 'form.button.search'|trans }}">
            <label class="input-group-text"><span class="bi bi-search" aria-hidden="true"></span></label>
            {% set autocomplete = prepare_attributes('global', 'game_autocomplete') %}
            <input class="form-control" type="text" {{ autocomplete|raw }} />
          </div>
        </div>
      </div>

      <div class="row my-4">
        {# Last games part #}
        <div class="col-12 col-md-6 mt-4 mt-md-0">
          <h4 class="text-center pb-2">{{ 'home.index.lastGame.title'|trans }}</h4>
          {% if (gamesInfo|length) > 0 %}
            {% for gameInfo in gamesInfo %}
              {% set game = gameInfo.game %}
              {% set gameId = 'game-' ~ game.id %}

              {# One card for each game #}
              <div id="{{ gameId }}" class="card-max-large mx-auto my-3 rounded-3 border border-primary bg-body overflow-hidden">
                <div class="row py-0 g-0 px-0">
                  {# Card title : no edit buttton on home page #}
                  <div class="col-12 border-bottom bg-primary">
                    {% set backpath = path('home_index', { anchor: gameId }) %}
                    {{ m.game_title(game, backpath, true, false) }}
                  </div>
                  {# Card image #}
                  <div class="col-12 border-bottom">
                    {{ m.game_image(game) }}
                  </div>

                  {# Card information #}
                  <div class="col-12">
                    <div class="row p-0 m-0">
                      {{ m.game_information(game) }}
                    </div>
                  </div>
                </div>
              </div>
            {% endfor %}
            {# Link to check all games list #}
            <div class="text-center p-2 p-md-4">
              <a href="{{ path('game_index') }}" class="btn btn-outline-secondary">{{ 'home.index.lastGame.all'|trans }}</a>
            </div>
          {% else %}
            <div class="text-center p-2 fs-5 fw-light">
              {{ 'home.index.lastGame.none'|trans }}
            </div>
          {% endif %}
        </div>

        {# Last reviews part #}
        <div class="col-12 col-md-6 mt-4 mt-md-0" {{ stimulus_controller('load-popover') }}>
          <h4 class="text-center pb-2">{{ 'home.index.lastReview.title'|trans }}</h4>

          {% if (reviews|length) > 0 %}
            {% for review in reviews %}
              {% set reviewId = 'review-' ~ review.id %}

              {# One card for each review #}
              <div id="{{ reviewId }}" class="card-max-large card-max-md-none mx-auto my-3 rounded-3 border border-primary overflow-hidden">
                {# Card title bar with the the title on one side, and 3 infos on the other side (or on top of each other on small screen) #}
                <div class="row gx-0 border-bottom text-center">
                  {# Actual title without the edit button #}
                  {% set popover = m.user_popover(review.user) %}
                  <div
                    class="col-4 border-bottom border-end border-end-md-0 d-flex p-1 ps-md-2 justify-content-center align-items-center text-light-emphasis bg-primary app-user-cell"
                    tabindex="0"
                    data-bs-toggle="popover"
                    data-bs-content="{{ popover|e }}"
                  >
                    {{ m.user_avatar(review.user) }} <span class="ms-2">{{ review.user.username }}</span>
                  </div>
                  <div class="col-8 border-bottom border-start-md bg-primary">
                    {% set backpath = path('home_index', { anchor: reviewId }) %}
                    {{ m.game_title(review.game, backpath, true, false) }}
                  </div>
                  {# 3 piece of information : player rating, hour spend, and first play date #}
                  <div class="col-12 d-flex align-items-center flex-even bg-body-tertiary">
                    {{ m.review_information(review) }}
                  </div>
                </div>

                {# Card body in one part : the comment #}
                <div class="px-2 py-1 bg-body">
                  {{ m.review_body(review, backpath) }}
                </div>
              </div>
            {% endfor %}
            {# Link to check all user reviews list #}
            <div class="text-center p-2 p-md-4">
              <a href="{{ path('review_index') }}" class="btn btn-outline-secondary">{{ 'home.index.lastReview.all'|trans }}</a>
            </div>
          {% else %}
            <div class="card-max-large mx-auto text-center my-1 p-2 fs-5 fw-light">
              {{ 'home.index.lastReview.none'|trans }}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </section>
{% endblock %}
